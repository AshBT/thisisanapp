"links" @node do |node|
  source node.source
  target node.target
  end
end

"nodes" @event do |event|
  classname event.classname
  event_name event.event_name
  file event.file
  id event.id
  line event.line
end
